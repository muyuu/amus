{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "実行",
            "command": "mise run start",
            "type": "shell",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "presentation": {
                "reveal": "always",
                "panel": "new",
                "close": true
            },
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ]
}
